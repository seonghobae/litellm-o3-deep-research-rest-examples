# One-day delivery plan

1. Bootstrap the repository structure and secret-safe defaults.
2. Implement the Python client with tests and a runnable CLI.
3. Implement the Java client with tests and a runnable CLI.
4. Add repository-level guidance (`AGENTS.md`, `ARCHITECTURE.md`, and `docs/**`).
5. Build and verify the Korean docs site when manuals change.
6. Verify both test suites and, when credentials are available, perform one successful live call per language.
7. Commit, push, and keep any PR or merge path consistent with `docs/workflow/pr-continuity.md`.
