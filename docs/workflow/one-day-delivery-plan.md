# One-day delivery plan

1. Bootstrap the repository structure and secret-safe defaults.
2. Implement the Python client with tests and a runnable CLI.
3. Implement the Java client with tests and a runnable CLI.
4. Implement the relay example with tests, a runnable Hypercorn entrypoint, and a structured tool-invocation contract.
5. Extend the Java example with relay caller mode.
6. Add repository-level guidance (`AGENTS.md`, `ARCHITECTURE.md`, and `docs/**`).
7. Build and verify the Korean docs site when manuals change.
8. Verify all test suites and, when credentials are available, perform successful direct and relay live calls.
9. Commit, push, and keep any PR or merge path consistent with `docs/workflow/pr-continuity.md`.
